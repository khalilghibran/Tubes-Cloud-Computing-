apiVersion: apps/v1
kind: Deployment
metadata:
  name: displayapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: displayapp
  template:
    metadata:
      labels:
        app: displayapp
    spec:
      containers:
        - name: displayapp
          image: yourdockerusername/displayapp:latest
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: "mariadb"
            - name: DB_USER
              value: "user"
            - name: DB_PASSWORD
              value: "password"
            - name: DB_NAME
              value: "library"
